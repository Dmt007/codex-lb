# Verification

TypeScript passes; 44 account-list, Accounts-page and model-source tests pass.
Chrome verifies gateway card placement inside account-list-scroll-region and
Add account > Company gateway transitioning to the editable source dialog.
Screenshots use fixture sources, not a registered live credential. Existing
source editing and switches reuse the Settings implementation. No additional
source panel appears beneath the account card. Source permissions are preserved.
